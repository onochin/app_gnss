# GNSS学習アプリ Codex共通作業ルール

## 1. 正本と作業開始

作業開始前に次を確認する。

1. 最新の`doc/HANDOFF.md`
2. 対象依頼Markdown
3. `git status --short`

既存差分を破棄、上書き、stash、restore、無関係に整形しない。

## 2. 実装方針

- 指定されたPhaseだけを実施する
- 最小差分とする
- 現在の章別コンポーネントと教材レジストリを維持する
- 既存の教材データ、純粋関数、UIを再利用する
- 不明な仕様を推測で確定しない
- 新規パッケージを勝手に追加しない
- 依存バージョンを勝手に更新しない

## 3. 必ず維持するGNSS仕様

- 実装済み第1章～第7章の文章、図、操作、固定教材値、計算結果
- 安定章ID、問題ID、選択肢ID
- 第1章～第7章の章順
- 章往復時のReact操作状態と問題回答の保持
- ブラウザ再読込み時の初期化
- GNSS用`localStorage`なし
- `.gnss-*` CSS名前空間
- 色だけに依存しない状態表示
- キーボード操作と可視フォーカス
- `NaN`、`Infinity`、`undefined`を画面へ表示しない

## 4. 変更禁止

- 指定前の第8章以降の実装
- GNSS学習記録・GNSS用`localStorage`の先行追加
- 外部API・実機通信・観測ファイル読込みの追加
- 新しいnpmパッケージの追加
- `package-lock.json`の依存バージョン更新
- リポジトリ名を推測したGitHub Pages baseの固定
- 指定前のremote追加・push・公開
- CSS全面再設計や大規模リファクタリング
- 既存スクリーンショットの上書き

## 5. UI

- 1366pxと390pxでページ全体を横にはみ出させない
- 通常説明文は13px、補助説明は12pxを基準とする
- 既存GNSSデザインと共通UIを維持する
- 章を切り替えても各章をアンマウントしない

## 6. 必須検証

最低限、次を実行する。

```bash
npm run typecheck -- --pretty false
npm test -- --reporter=verbose
npm run build
node --check scripts/gnss-smoke.mjs
git diff --check
```

可能な場合はGNSS Playwrightスモークで次も確認する。

- 第1章～第7章への移動
- 主要操作と確認問題
- 章往復時の状態保持
- 再読込み後の初期化
- 1366px・390px表示
- キーボード操作・可視フォーカス
- コンソールエラー0件
- ページ例外0件
- 外部API通信0件

必要な確認画像は`/tmp`へ新しい名前で保存する。

## 7. HANDOFF更新

作業完了時に`doc/HANDOFF.md`へ、実装内容、変更ファイル、維持事項、検証結果、
警告、残る注意点、次回開始地点を追記する。

## 8. 完了報告

日本語で、結論、変更内容、検証結果、注意点の順に簡潔に報告する。
テスト数、ビルドmodules数、成果物サイズ、警告の有無も含める。
