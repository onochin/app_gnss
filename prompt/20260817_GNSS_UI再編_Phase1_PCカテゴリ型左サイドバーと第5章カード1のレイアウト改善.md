# GNSS UI再編 Phase 1
## PCカテゴリ型左サイドバーと第5章カード1のレイアウト改善

## 0. 今回の目的

GNSS学習アプリは独立化 Phase 2まで完了し、

- GitHub repository：`onochin/app_gnss`
- GitHub Pages公開済み
- GNSS第1章～第7章 available
- 第8章以降 未実装

の状態です。

今回、第8章「自前RTK④ 現場観測と点検」の実装へ進む前に、
GNSS教材全体のナビゲーションと一部レイアウトを整理します。

今回の主目的は次の2点です。

1. PC版の章ナビゲーションを、現在の上部横並び型から「カテゴリ型左サイドバー」へ再編する
2. 第5章カード1の章全体フロー等が縦方向に間延びしているため、教材内容を変えず、PCでよりコンパクトに理解できる表示へ改善する

今回は教材内容そのものを改稿するPhaseではありません。

第8章以降の教材本体は実装しないでください。

また今回はユーザーがローカル画面を目視確認するため、
まずローカル実装・検証まで行い、commit / push / Pages公開は行わず停止してください。

---

# 1. 作業場所

作業ディレクトリ：

```text
/home/newono/ai_proj/app_simulation/gnss-learning-lab
```

このGNSS独立アプリだけを変更してください。

移行元：

```text
/home/newono/ai_proj/app_simulation/survey-learning-lab
```

は変更・commit・pushしないでください。

---

# 2. 正本・優先順位

作業開始前に必ず次を確認してください。

1. 最新の `doc/HANDOFF.md`
2. `AGENTS.md`
3. `README.md`
4. 現在の実装
5. `src/components/gnss/SurveyGnss.tsx`
6. `src/components/gnss/gnssCourse.ts`
7. `src/components/gnss/types.ts`
8. `src/components/gnss/lessons/GnssOwnBaseStationLesson.tsx`
9. 第5章教材データ
10. `src/styles.css`
11. GNSS関連単体テスト
12. `scripts/gnss-smoke.mjs`
13. 本依頼

内容が異なる場合は、

```text
現在のGNSS実装
↓
最新 doc/HANDOFF.md
↓
AGENTS.md
↓
本依頼
↓
過去資料
```

を基本に判断してください。

---

# 3. 作業開始時の安全確認

最初に最低限、次を確認してください。

```bash
pwd
git status --short
git branch --show-current
git log --oneline --decorate -5
git remote -v
git rev-parse --show-toplevel
```

Gitルートが、

```text
/home/newono/ai_proj/app_simulation/gnss-learning-lab
```

であることを確認してください。

branchは `main`、remoteは `onochin/app_gnss` を想定しています。

ユーザーが意図していない未commit差分がある場合は、
破棄・stash・restore・上書きをせず停止して報告してください。

禁止：

```text
git reset
git checkout -- .
git restore .
git clean
```

---

# 4. 実装開始条件

最新HANDOFFと現在実装から、少なくとも次を確認してください。

```text
第1章 gnss-overview
第2章 gnss-observations
第3章 gnss-coordinate-height
第4章 gnss-positioning-methods
第5章 gnss-own-base-station
第6章 gnss-correction-delivery
第7章 gnss-baseline-fix
```

がavailableであること。

さらに、

```text
第8章以降の教材本体
GNSS学習記録
GNSS用localStorage
```

が未実装であること。

想定と異なる場合は実装を開始せず報告してください。

---

# 5. 最重要：第1章～第7章の教材内容を変更しない

今回変更する主対象は、

```text
ナビゲーション
レイアウト
余白
第5章カード1の見取り図の表示方法
```

です。

現在の第1章～第7章について、次を変更しないでください。

- 教材本文
- 各カードの意味
- 数値
- 図の教育内容
- 計算
- 確認問題
- 正答
- 問題ID
- 選択肢ID
- 安定章ID
- 固定教材値
- React操作状態
- 問題回答状態
- 理解済み状態の意味

特に安定章ID：

```text
gnss-overview
gnss-observations
gnss-coordinate-height
gnss-positioning-methods
gnss-own-base-station
gnss-correction-delivery
gnss-baseline-fix
```

を変更しないでください。

---

# 6. 最重要：現在の状態保持方式を維持する

現在の `SurveyGnss` は、
第1章～第7章のLessonをアンマウントせず保持し、
非選択章を非表示にすることで章往復時の状態を維持しています。

今回の左サイドバー化を理由に、

```text
activeLessonだけを条件レンダリング
↓
章変更時にアンマウント
```

する構成へ変更しないでください。

現在仕様：

```text
章を往復
→ 操作状態・問題回答等を保持

ブラウザ再読込み
→ 初期化
```

をそのまま維持してください。

GNSS用localStorageは追加しないでください。

---

# 7. PC版：カテゴリ型左サイドバー

## 7.1 基本レイアウト

PC版では、Header下を原則として、

```text
┌─────────────────────────────────────────────┐
│ Header                                      │
├──────────────┬──────────────────────────────┤
│ 左サイド     │                              │
│              │        GNSS教材本文          │
│              │                              │
└──────────────┴──────────────────────────────┘
```

の2カラム構成としてください。

左サイドバー幅は、

```text
約248px
```

を第一候補としてください。

実装上必要であれば概ね、

```text
236～260px
```

の範囲で調整して構いません。

本文を不必要に狭くしないことを優先してください。

PCでは左サイドバーを `sticky` とし、
ページを縦スクロールしても章ナビゲーションへ戻りやすくしてください。

Headerと重ならないよう、現在のHeader寸法を確認してsticky位置を決めてください。

固定高さを推測で設定しないでください。

---

# 8. 左サイドバーの教材カテゴリ

カテゴリは次の順序としてください。

```text
基礎編
基準局RTK
ネットワークRTK
CLAS測量
スタティック測量
後処理
応用編
```

現時点での章配置は次とします。

## 基礎編

```text
01  GNSS測量の全体像
02  GNSSは何を観測しているのか
03  GNSSの座標と高さ
04  GNSS測位方式を比較する
```

内部的には既存の正式タイトルと安定章IDを使用してください。

サイドバーでは、横幅に合わせて重複語を省略した短い表示名を使用して構いません。

例：

```text
01  全体像
02  GNSSは何を観測しているのか
03  座標と高さ
04  測位方式を比較する
```

ただし教材本文の正式タイトルは変更しないでください。

## 基準局RTK

```text
05  基準局をつくる
06  補正情報を届ける
07  基線解析とFIX
08  現場観測と点検
```

第5章～第7章は既存章です。

第8章はまだ未実装なので、

```text
08  現場観測と点検
    準備中
```

と表示してください。

第8章の章ID、Lesson、カード、問題等は今回作成しないでください。

---

# 9. 未実装カテゴリ

次はカテゴリだけ表示してください。

```text
ネットワークRTK
CLAS測量
スタティック測量
後処理
応用編
```

現時点では中に具体的な新章番号を割り当てないでください。

将来ロードマップの章番号を今回のUI実装で先に確定しないこと。

各カテゴリには、

```text
準備中
```

程度を表示してください。

未実装教材をクリック可能なLessonとして作らないでください。

未実装項目は、

- buttonにしない
- activeLessonIdへ設定しない
- 架空のLesson IDを作らない

方針を基本としてください。

---

# 10. カテゴリ開閉

PCのカテゴリは折りたためる構成としてください。

例：

```text
▼ 基礎編
▶ 基準局RTK
▶ ネットワークRTK
▶ CLAS測量
▶ スタティック測量
▶ 後処理
▶ 応用編
```

現在選択中の章を含むカテゴリは、
現在章が分かるよう開いた状態にしてください。

例：

第2章を選択中：

```text
▼ 基礎編
   01 ...
   02 ... ← current
   03 ...
   04 ...

▶ 基準局RTK
```

第6章を選択中：

```text
▶ 基礎編

▼ 基準局RTK
   05 ...
   06 ... ← current
   07 ...
   08 ... 準備中
```

章選択によって別カテゴリへ移動した場合、
移動先カテゴリが開くようにしてください。

不要に複雑な開閉状態管理にはしないでください。

---

# 11. 選択中章の表示

現在章は、

- 背景
- 左ラインまたはマーカー
- フォントウェイト
- `aria-current="page"`

等を組み合わせて識別できるようにしてください。

色だけで選択状態を表現しないでください。

既存の可視フォーカスを維持してください。

---

# 12. モバイル版

390px程度の画面では、
PC用左サイドバーを常設しないでください。

本文幅を優先してください。

モバイルでは、

```text
現在：
第5章 基準局をつくる

［章を選ぶ ▼］
```

のようなコンパクトな章ナビゲーションとしてください。

開いた場合は、PC版と同じカテゴリ構造を縦方向に表示してください。

例：

```text
基礎編
  01 ...
  02 ...
  03 ...
  04 ...

基準局RTK
  05 ...
  06 ...
  07 ...
  08 ... 準備中
```

新規UIライブラリは追加しないでください。

既存React / HTML / CSSだけで実装してください。

`details / summary`、button + region等、
現在のアクセシビリティ方針に合う最小構成を選択してください。

---

# 13. 第5章カード1の間延び改善

今回のもう1つの主要修正です。

対象：

```text
第5章
gnss-own-base-station

カード1
「自前RTKの基準局をつくる」
```

現在、このカードの章全体見取り図は、

```text
基準局座標を決める
↓
その座標の点とアンテナを結び付ける
↓
GNSSを安定して観測できる場所へ設置する
↓
基準局として使う前に確認する
↓
次は基準局の情報を移動局へ届ける
```

を、横幅いっぱいの大きな縦5段フローとして表示しています。

内容は変更せず、
PCでは章全体が一目で把握できるコンパクトなステップ表示へ変更してください。

---

# 14. 第5章カード1：PC用5ステップ表示

PCでは概念として、

```text
01
基準局座標を決める
      →

02
その座標の点と
アンテナを結び付ける
      →

03
GNSSを安定して観測できる
場所へ設置する
      →

04
基準局として
使う前に確認する
      →

05
次は基準局の情報を
移動局へ届ける
```

を横方向に並べてください。

表示イメージ：

```text
┌────────┐ → ┌────────┐ → ┌────────┐ → ┌────────┐ → ┌────────┐
│ 01     │   │ 02     │   │ 03     │   │ 04     │   │ 05     │
│ 座標   │   │ 点と   │   │ 観測   │   │ 使用前 │   │ 情報を │
│ を決定 │   │アンテナ│   │ 環境   │   │ 確認   │   │ 届ける │
└────────┘   └────────┘   └────────┘   └────────┘   └────────┘
```

これはレイアウトイメージであり、
教材本文を短縮する指示ではありません。

現在の `gnssOwnBaseStationOverviewFlow` の内容を基本的にそのまま表示してください。

---

# 15. 第5章カード1以外のフローを変更しない

現在の `GnssOwnBaseFlow` は、
カード1以外の具体的な処理フローでも使用されています。

今回、

```text
すべての縦フロー
→ 横型
```

へ変更しないでください。

役割を次のように分けます。

```text
章全体の見取り図
→ コンパクトな横ステップ

具体的な作業・処理手順
→ 現在の縦フローを維持
```

実装方法は最小差分を優先してください。

例えば、

```text
GnssOwnBaseFlowへoverview用variantを追加
```

または、

```text
カード1だけ専用のOverviewFlowを作成
```

等が考えられます。

どちらを採るかは現在コードを確認して、
既存カードへの影響が最も小さい方法を選択してください。

大規模リファクタリングは不要です。

---

# 16. レスポンシブ表示

第5章カード1の見取り図は、

```text
広いPC
→ 5ステップを横方向

中間幅
→ 必要に応じて複数行

スマホ
→ 1列の縦方向
```

としてください。

1366pxでは、
左サイドバーを表示した状態でも不自然な横スクロールを発生させないこと。

390pxでは1列または十分読みやすい縦配置にしてください。

PCで5列を無理に詰め込み文字が極端に狭くなる場合は、
現在の実幅を確認して最適な折返しを選択してください。

---

# 17. オレンジ色の重要メッセージ

第5章カード1の現在の重要メッセージ、

```text
正しい基準局をつくるには、基準となる座標を決め、
その座標とGNSSアンテナを正しく結び付け、
安定して観測できる状態にする必要があります。
```

は内容を変更せず維持してください。

ただし、第5章カード1内の過剰な上下余白は、
他カードへ影響しない範囲で少し整理して構いません。

グローバルな `.gnss-card` 等を変更して、
全章の余白を一括変更しないでください。

---

# 18. 「GNSS受信機を設置できた ≠ 適切な基準局」の改善

現在の、

```text
GNSS受信機を設置できた
≠
測量成果の基準として適切な基準局をつくれた
```

という重要比較は内容を維持してください。

PCでは、可能であれば1つのコンパクトな比較行として、

```text
GNSS受信機を設置できた
       ≠
測量成果の基準として適切な基準局をつくれた
```

を横方向に表示してください。

スマホでは無理に横一列へせず、

```text
GNSS受信機を設置できた
          ≠
測量成果の基準として適切な基準局をつくれた
```

の縦配置で構いません。

意味を弱めないでください。

---

# 19. CSS方針

既存 `.gnss-*` 名前空間を維持してください。

今回追加するCSSも原則として `.gnss-*` 配下としてください。

禁止：

- CSS全面再設計
- 既存GNSS配色の大幅変更
- 全教材カードの余白を一括変更
- 大規模な未使用CSS削除
- 第8章以降を想定した大量の先行CSS
- UIライブラリ導入

既存のGNSSデザインと馴染む最小差分にしてください。

---

# 20. App.tsx / SurveyGnss.tsx

現在の、

```text
App
↓
SurveyGnss
↓
第1章～第7章
```

という構造は維持してください。

左サイドバー実装のために必要であれば、
ナビゲーション部分を小さなコンポーネントへ分けて構いません。

ただし、

- 不必要なアプリ構造変更
- routingライブラリ導入
- URL routingの新規実装
- Lessonコンポーネントの大規模再構成

は行わないでください。

---

# 21. 第8章は実装しない

左サイドバーには、

```text
08 現場観測と点検
準備中
```

を表示しますが、

今回次は実装しないでください。

- 第8章Lesson
- 第8章データ
- 第8章カード
- 第8章問題
- 第8章IDの正式追加
- 第8章状態管理
- 第8章CSS
- 第8章テスト

第8章は別チャットで教材内容を整理中であり、
正式依頼後に実装します。

---

# 22. 新規パッケージ禁止

新規npmパッケージを追加しないでください。

次を変更しないでください。

```text
package.json の依存構成
package-lock.json の依存バージョン
```

React / CSS / HTMLで実装してください。

---

# 23. 単体テスト

既存GNSSテストを退行させないでください。

必要な範囲で、
今回のナビゲーション再編に対するテストを追加してください。

最低限確認したいもの：

1. 第1章～第7章の安定章IDが維持される
2. 第1章～第7章の章順が維持される
3. 基礎編に第1～4章が属する
4. 基準局RTKに第5～7章が属する
5. 第8章「現場観測と点検」は準備中でありactiveLessonにならない
6. 未実装カテゴリから架空Lessonが生成されない
7. 第1～7章の既存Lesson切替が正常
8. 既存問題・純粋関数テストがすべて成功

テストのためだけに本番コードを不自然に複雑化しないでください。

---

# 24. Playwright / スモーク

`scripts/gnss-smoke.mjs` を現在構成に合わせて必要最小限更新してください。

最低限次を確認してください。

## 1366px

- PC左サイドバーが表示される
- 基礎編が表示される
- 基準局RTKが表示される
- 第1～7章へ移動できる
- 選択章に `aria-current="page"` 相当がある
- 第8章は準備中で選択できない
- カテゴリ開閉がキーボードでも操作できる
- 第5章カード1の5ステップが過度に縦長でない
- ページ全体に横スクロールがない

## 390px

- PC用左サイドバーを常設しない
- コンパクト章ナビゲーションを利用できる
- 第1～7章へ移動できる
- 第8章は準備中
- 第5章カード1のフローが読みやすく縦配置される
- 横スクロールがない

## 状態保持

既存どおり、

```text
第1章で操作
↓
別章へ移動
↓
第1章へ戻る
↓
状態保持
```

を確認してください。

再読込み後は初期化されることも維持してください。

## エラー

- console error 0件
- page error 0件
- 外部API通信0件
- `NaN`
- `Infinity`
- `undefined`

を画面へ表示しないこと。

---

# 25. 必須ローカル検証

最低限次を実行してください。

```bash
npm run typecheck -- --pretty false
npm test -- --reporter=verbose
npm run build
npm run build -- --mode github-pages
node --check scripts/gnss-smoke.mjs
git diff --check -- . ':(exclude)prompt/**'
```

可能な場合はGNSS Playwrightスモークも実施してください。

新しいスクリーンショットが必要な場合は `/tmp` へ保存し、
既存スクリーンショットを上書きしないでください。

最低限、

```text
1366px 第5章カード1
390px 第5章カード1
1366px 左サイドバー全体
390px モバイル章選択
```

を目視確認してください。

---

# 26. GitHub Pages設定を変更しない

今回のUI再編を理由に、

```text
vite.config.ts のPages base
.github/workflows/deploy.yml
GitHub repository設定
Pages設定
```

を変更しないでください。

現在の `/app_gnss/` Pages構成を維持してください。

---

# 27. 今回はcommit / pushしない

今回はユーザーによるローカル画面確認を挟みます。

そのため、実装と検証が成功しても、

```text
git commit
git push
gh repo ...
GitHub Pages公開
```

を行わないでください。

ユーザーが、

```text
npm run dev
```

でPC・スマホ表示を確認した後、
必要であればレイアウトを微調整します。

ユーザー承認後に、別途、

- 最終検証
- commit
- push
- GitHub Actions
- Pages公開確認
- README / HANDOFF更新

を行います。

---

# 28. 今回はHANDOFFを最終更新しない

`AGENTS.md`では通常、Phase完了時にHANDOFF更新を行います。

ただし今回は、

> UI実装後のユーザー目視確認前のチェックポイント

です。

したがって、この実行をPhase完了扱いにせず、
`doc/HANDOFF.md` の最終更新はまだ行わないでください。

READMEもまだ更新しないでください。

ユーザー確認後、公開反映まで完了した時点で更新します。

---

# 29. 今回変更しないもの

今回、次は行わないでください。

- 第8章以降の教材実装
- 第1章～第7章教材本文の改稿
- 確認問題内容変更
- 既存ID変更
- GNSS学習記録
- GNSS用localStorage
- URL routing導入
- React Router等の追加
- 新規npmパッケージ
- 依存更新
- CSS全面整理
- JS 500kB警告だけを理由としたcode splitting
- Playwrightブラウザ再取得
- npm cache共有方式変更
- GitHub Pages設定変更
- repository設定変更
- commit
- push
- Pages公開
- README最終更新
- HANDOFF最終更新

---

# 30. 完了時の報告

ローカル実装・検証が完了したら、そこで停止してください。

日本語で簡潔に次を報告してください。

1. 結論
2. 変更ファイル
3. PC左サイドバーの実装内容
4. モバイル章選択の実装内容
5. 第5章カード1の改善内容
6. 第1章～第7章が変更されていない範囲
7. 第8章が未実装のままであること
8. 型検査結果
9. 単体テスト結果
10. 通常build結果
11. Pages build結果
12. Playwright結果
13. 1366px / 390pxのoverflow結果
14. console error / page error
15. `git status --short`
16. ユーザーが `npm run dev` で確認すべきポイント

commit / pushせず、
ユーザーの目視確認を待ってください。
